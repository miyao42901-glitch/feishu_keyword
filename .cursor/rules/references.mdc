---
description: references 目录为只读参考资料，禁止任何修改
alwaysApply: true
---

# `references/` 目录使用约束

`references/` 目录用于存放**外部参考资料 / 历史代码 / 第三方示例**，仅供查看和借鉴，**不属于本项目源码**。

## 允许的操作

- 使用 `Read` / `Grep` / `Glob` / `SemanticSearch` 等只读工具阅读、检索其中的文件。
- 在分析、设计、回答问题时，引用其中的代码片段或思路作为参考。

## 禁止的操作

- **禁止**使用 `Write` / `StrReplace` / `Delete` / `EditNotebook` 等工具创建、修改、删除 `references/` 下的任何文件或子目录。
- **禁止**通过 `Shell` 执行任何会改动 `references/` 内容的命令（如 `mv`、`rm`、`cp` 写入、`git` 改写、重定向 `>` / `>>` 等）。
- **禁止**将 `references/` 中的文件提交到 git（已在 `.gitignore` 中忽略，不要再使用 `git add -f` 强制加入）。
- **禁止**把 `references/` 中的文件移动 / 复制到项目其它目录“顺便修改”——如确需借用，**重写实现**，不要直接搬运。

## 例外处理

如果用户**明确要求**修改 `references/` 下的内容，必须先口头确认：

> "`references/` 目录按规则是只读参考资料，你确认要修改其中的文件吗？"

得到肯定答复后方可操作；否则一律按只读处理。

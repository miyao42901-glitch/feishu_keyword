---
description: 业务改动仅在个人拼音缩写分支（如 hxp）；切换其它分支前须 git reset --hard
alwaysApply: true
---

# 分支约束（个人拼音缩写分支）

## 硬性约定

- **所有业务代码与配置的修改、提交，只允许发生在自己的个人分支上**（分支名为**拼音缩写小写**，如 `hxp`、`zjl`）。不要默认在 `test`、`master` 或其它人分支上改代码。
- 开始编辑或运行可能改写仓库的命令前，应确认当前分支为**你的个人分支**（例如 `git branch --show-current`）。**若当前不是个人分支**：先向用户说明，请用户切换到个人分支后再继续；**不要**在 `test`/`master` 上代为提交业务改动。
- 完整流程（merge 到 `test`、MR 发正式）见 **`docs/GIT_WORKFLOW.md`**。

## 切换到其它分支之前

用户或你需要**进入其它分支**（查看、对比、合并 `test` 等）时：

1. 在当前分支执行 **`git reset --hard`**，将**已跟踪文件**的未提交修改全部丢弃，与工作区最后一次提交一致。
2. 再执行 `git switch <其它分支>` / `git checkout <其它分支>`。

说明：

- `git reset --hard` **不会**删除未跟踪文件（untracked）。若存在不应提交的未跟踪目录/文件，需单独处理（例如删除或加入 `.gitignore`）；仅在用户明确要求且确认无重要未跟踪内容时，才可建议使用 `git clean -fd`（**会删除未跟踪文件，慎用**）。
- 若用户**必须保留**当前未提交改动，应使用 `git stash push -u` 等由用户明确选择的方式；**不得**在未获用户确认时用 `reset --hard` 丢弃其可能想保留的修改。

## 回到个人分支后继续开发

- 从其它分支回到个人分支：`git switch hxp`（换成你的分支名），再按计划修改；确保最终变更与提交都在个人分支上完成。
- 推测试栈前须在本地 **merge 到 `test` 再 push**，禁止 `git push origin hxp:test` 跳过本地合并（见 `docs/GIT_WORKFLOW.md`）。

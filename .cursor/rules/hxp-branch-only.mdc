---
description: 切换分支前须 git reset --hard；推测试栈须本地 merge 到 test 再 push
alwaysApply: true
---

# 分支操作规范

## 切换到其它分支之前

需要切换分支时：

1. 在当前分支执行 **`git reset --hard`**，将已跟踪文件的未提交修改全部丢弃。
2. 再执行 `git switch <目标分支>` / `git checkout <目标分支>`。

说明：

- `git reset --hard` **不会**删除未跟踪文件（untracked）。若存在不应提交的未跟踪目录/文件，需单独处理；仅在用户明确要求且确认无重要未跟踪内容时，才可建议使用 `git clean -fd`（**会删除未跟踪文件，慎用**）。
- 若用户**必须保留**当前未提交改动，应使用 `git stash push -u` 等由用户明确选择的方式；**不得**在未获用户确认时用 `reset --hard` 丢弃其可能想保留的修改。

## 推送规范

- 推测试栈前须在本地 **merge 到 `test` 再 push**，禁止 `git push origin <branch>:test` 跳过本地合并（见 `docs/GIT_WORKFLOW.md`）。
- 不要直接向 `master` 推送业务改动，走 GitLab MR 流程。

# 本机 / 局域网联调覆盖（复制为仓根 .env.local，勿提交 Git）
# 先：cp .env.test .env  再：cp .env.local.example .env.local

ENVIRONMENT=local

DATABASE_URL=mysql+pymysql://root:PASSWORD@127.0.0.1:3306/feishu_keyword?charset=utf8mb4

REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_URL=redis://127.0.0.1:6379/0

HTTP_HOST=0.0.0.0
HTTP_PORT=8765
HTTP_RELOAD=1
CELERY_TASK_ALWAYS_EAGER=0
DATABASE_RUN_MIGRATIONS=1
ASYNC_TASK_DB_AUTO_CREATE=0
ASYNC_DISPATCH_HTTP_ENABLED=1
ASYNC_SCHEDULE_BEAT_ENABLED=0

VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_ADMIN_API_ORIGIN=http://127.0.0.1:8000
YDDM_PROXY_TARGET=https://api.yddm.com
# 局域网联调 python sync-api；测试飞书域 /api/v1 修好后可改为 https://test-fskw-feishu.tbpf.com
SYNC_PROXY_TARGET=http://127.0.0.1:8765

# 对齐线上测试/正式域本地 dev 时：先 cp .env.test 或 .env.master 为 .env，再按需改本文件中的 VITE_* / SYNC_PROXY_TARGET

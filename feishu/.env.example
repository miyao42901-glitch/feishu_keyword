# 前端环境变量模板（可提交 Git）
#
# | 场景 | 用法 |
# |------|------|
# | 本机回环 | npm run dev:local 或 cp 后改 .env.local |
# | 局域网联调（手机/同事访问） | cp .env.local.example .env.local，npm run dev:lan |
# | 测试/正式打包 | npm run build:public:test / :prod |
#
# 前端直连后端 API 的根地址（不含末尾 /，不含 /api）
VITE_API_BASE_URL=http://127.0.0.1:8000

# 首页横幅「操作文档」按钮跳转的外链（可选；未配置时使用代码内默认飞书 Wiki）
# VITE_OPERATIONS_DOC_URL=https://lcnnrhjmwxym.feishu.cn/wiki/Hnstw1DDgi3sswkMwh6cyn2lnlc

# 首页横幅「加入用户群」外链（可选；未配置时使用代码内默认飞书群链接）
# VITE_USER_GROUP_URL=https://applink.feishu.cn/client/chat/chatter/add_by_link?link_token=8aevca35-ad2f-4177-ba22-26a3413a852c

# YDDM 计费 / 登录。默认不配置即走同源 /yddm-api（Vite 或 Nginx 反代到 YDDM 上游）
# VITE_YDDM_API_BASE=/yddm-api
# 内网直连 YDDM 且已开 CORS 时：
# VITE_YDDM_API_BASE=http://192.168.1.11:8001
#
# 仅 npm run dev：Vite 将 /yddm-api 转发到下列上游（勿与 VITE_YDDM_API_BASE=/yddm-api 混用）
# YDDM_PROXY_TARGET=http://192.168.1.11:8001
#
# 注册图片验证码：`GET {VITE_YDDM_API_BASE}/auth/get_captcha?phone_num=…&t=…`（浏览器 img 直接加载）

# 登录弹框底部「客服二维码」图片地址（可选；未配置时使用 public/custom.png）
# VITE_CUSTOMER_SERVICE_QR_URL=https://example.com/support-qr.png

# 同步采集服务根地址（不含末尾 /）
# 推荐：不配置，走同源 /api/v1 → Vite 或 Nginx 反代到 8765（见 docs/feishu/NGINX.md）
# 仅在内网直连且已开 CORS 时：
# VITE_SYNC_API_BASE=http://192.168.1.11:8765
#
# 定时任务：
# - `POST /api/v1/async/tasks` 提交（监控窗口 + 频率 + 每次条数 + body）
# - `GET /api/v1/async/tasks` 列表（轮询状态/结果，可选 `?task_ids=`）
# - `GET /api/v1/async/tasks/{task_id}` 查看任务（Header：x-api-key、X-User-Id；Query 可选 X-API-KEY）
# - `GET /api/v1/async/tasks/{task_id}/results` 单条结果（先 GET 状态；query 可选 page）
# - `POST /api/v1/results/acceptance` 结果验收（body：`douyin`/`xhs` 为异步任务 id 整数数组）
# 入参示例：action、body{keyword,sort_type…}、task_start_time、task_end_time、interval_minutes、fetch_count
# VITE_ASYNC_TASK_ACTION_DOUYIN=douyin-search-all
# VITE_ASYNC_TASK_ACTION_XHS=xhs-search-all
# VITE_ASYNC_TASK_ACTION_WXVIDEO=wxvideo-search-all
# VITE_ASYNC_TASK_ACTION_GZH=mp-search-all

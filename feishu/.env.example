# 前端直连后端 API 的根地址（不含末尾 /，不含 /api）
# 本地开发可不配置：默认 http://127.0.0.1:8000；打包部署前建议显式填写真实后端地址
VITE_API_BASE_URL=http://127.0.0.1:8000

# 首页横幅「操作文档」按钮跳转的外链（可选；未配置时点击会提示需设置）
# VITE_OPERATIONS_DOC_URL=https://example.com/your-doc

# yddm 计费（与上项无关）。不设时：npm run dev 走 /yddm-api → Vite 代理到 https://api.yddm.com，避免 CORS；
# 生产构建默认直连 https://api.yddm.com；若飞书内嵌仍报 CORS，请让对方网关放行插件页面 Origin，或填你可控反代根地址。
# VITE_YDDM_API_BASE=https://api.yddm.com
#
# 注册图片验证码：`GET {VITE_YDDM_API_BASE}/auth/get_captcha?phone_num=…&t=…`（浏览器 img 直接加载）

# 登录弹框底部「客服二维码」图片地址（可选；未配置时显示占位文案）
# VITE_CUSTOMER_SERVICE_QR_URL=https://example.com/support-qr.png

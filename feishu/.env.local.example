# 复制为 feishu/.env.local 后 npm run dev
#
# 与线上一致（推荐二选一）：
#   .env.local.prod.example  → 正式 fskw-feishu.tbpf.com（同 build:public:prod）
#   .env.local.test.example  → 测试 test-fskw-feishu.tbpf.com（同 build:public:test）
#
# 或直接用脚本（会覆盖 .env.local 里同名项）：
#   npm run dev:prod
#   npm run dev:test
#   npm run dev:local   # 本机 8000 + 内网 8765

node_modules/

# public：由 CI Runner 编译后 tar 部署；本地 npm run build:public:* 产物勿提交
/public/

# Docker 本地数据卷
/data/

# 参考资料目录（仅本地查看，勿提交）
/references/

# Python（server/ 后端）
__pycache__/
*.py[cod]
*$py.class
.idea/
.vscode/
server/.env
server/**/__pycache__/
server/**/*.py[cod]
server/**/*$py.class
server/**/*.so
server/.Python
server/venv/
server/.venv/
server/ENV/
server/env/
server/.eggs/
server/**/*.egg-info/
server/**/*.egg
server/dist/
server/build/
server/.pytest_cache/
server/.mypy_cache/
server/.ruff_cache/
server/.coverage
server/htmlcov/
server/**/*.cover
server/.hypothesis/

# ------------------------------------------------------------------
# 本地访问 / 私密配置（数据库连接、本机 API、个人 token 等，勿提交）
# ------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.development
.env.production
# 仓根与任意子目录下的 .env（保留模板与 test/master 占位版）
**/.env
!.env.example
!.env.test
!.env.master
!.env.local.example
!**/.env.example
!**/.env.sample
scripts/reset-root.sql

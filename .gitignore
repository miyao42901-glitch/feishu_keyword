# 本地残留目录（勿提交）
/web/

# ------------------------------------------------------------------
# 本地访问 / 私密配置（数据库连接、本机 API、个人 token 等，勿提交）
# ------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.development
.env.production
# 任意子目录下的环境文件（保留 *.example / *.sample 模板）
**/.env
!**/.env.example
!**/.env.sample
